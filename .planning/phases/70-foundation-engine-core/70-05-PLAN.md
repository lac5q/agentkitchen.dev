---
phase: 70-foundation-engine-core
name: HIL edit-and-continue — TypeScript route, client, and edit UI
plan: "05"
type: execute
autonomous: true
wave: 3
depends_on: ["70-02"]
files_modified:
  - apps/memroos/src/app/api/orchestration/hil/[id]/edit/route.ts
  - apps/memroos/src/lib/orchestration/client.ts
  - apps/memroos/src/components/orchestration/HilEditPanel.tsx
  - apps/memroos/src/app/api/orchestration/hil/[id]/edit/__tests__/route.test.ts
  - apps/memroos/src/lib/orchestration/__tests__/client.test.ts
requirements:
  - HIL-01
  - HIL-02
  - HIL-03
requirements_addressed:
  - HIL-01
  - HIL-02
  - HIL-03

must_haves:
  truths:
    - "An operator can open a paused HIL task, edit its declared OrchestrationState fields in a dedicated UI, and submit the edit"
    - "The TS edit route enforces authorizeRegistryWrite and proxies the validated patch to the Python service"
    - "Schema validation rejection from the Python service surfaces to the operator before resume"
  artifacts:
    - path: "apps/memroos/src/app/api/orchestration/hil/[id]/edit/route.ts"
      provides: "PATCH /api/orchestration/hil/{id}/edit — auth-guarded proxy to Python service"
      contains: "authorizeRegistryWrite"
    - path: "apps/memroos/src/lib/orchestration/client.ts"
      provides: "editOrchestrationHil() client function"
      contains: "editOrchestrationHil"
    - path: "apps/memroos/src/components/orchestration/HilEditPanel.tsx"
      provides: "Operator edit UI for paused HIL task state"
  key_links:
    - from: "HilEditPanel.tsx"
      to: "editOrchestrationHil"
      via: "form submit handler"
      pattern: "editOrchestrationHil"
    - from: "PATCH /api/orchestration/hil/{id}/edit"
      to: "Python PATCH /hil/{id}/edit"
      via: "authorized proxy fetch"
      pattern: "/hil/.*/edit"
---

<objective>
Complete HIL-01..03 with the TypeScript surface: an auth-guarded `PATCH /api/orchestration/hil/{id}/edit` route that proxies to the Python edit endpoint (Plan 02), an `editOrchestrationHil()` client function, and a dedicated operator edit UI. This plan depends on Plan 02 — the Python edit endpoint must exist before this proxy can call it.

Purpose: Plan 02 delivered the Python orchestration-service half. RESEARCH.md prescribes the Next.js half: the TS route proxies the edit to the Python service and owns operator authorization via `authorizeRegistryWrite` (RESEARCH.md Pitfall 5, CC-5 — omitting the guard creates an open endpoint reachable through the Cloudflare tunnel). The UI lets an operator edit a paused task's declared state fields before resuming.
Output: The TS edit route, the `editOrchestrationHil()` client, and the `HilEditPanel` operator UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-foundation-engine-core/70-RESEARCH.md
@.planning/phases/70-foundation-engine-core/70-CONTEXT.md
@CLAUDE.md
@apps/memroos/src/lib/orchestration/client.ts
@apps/memroos/src/app/api/orchestration/hil/route.ts
@apps/memroos/src/app/api/orchestration/hil/[id]/route.ts
@.planning/phases/70-foundation-engine-core/70-02-SUMMARY.md

<interfaces>
<!-- Plan 02 (Python) delivers: PATCH /hil/{id}/edit accepting a HilEditRequest
     body (fields mirror editable OrchestrationState; extra keys -> 422),
     returns 200 {ok: true, editedFields: [...]}.
     Confirm the exact request/response shape against 70-02-SUMMARY.md. -->

Existing TS client (apps/memroos/src/lib/orchestration/client.ts):
  interface OrchestrationHilDecision { ... }
  resolveOrchestrationHil(id, decision)   -- pattern to mirror for editOrchestrationHil

Auth guard pattern (apps/memroos/src/app/api/orchestration/hil/route.ts):
  authorizeRegistryWrite(request)  -> false when unauthorized
  registryWriteUnauthorizedResponse()  -> 403 response
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: editOrchestrationHil client function</name>
  <read_first>
    - apps/memroos/src/lib/orchestration/client.ts — resolveOrchestrationHil signature and fetch pattern to mirror
    - .planning/phases/70-foundation-engine-core/70-02-SUMMARY.md — confirm the Python edit request/response contract
  </read_first>
  <files>apps/memroos/src/lib/orchestration/client.ts</files>
  <behavior>
    - editOrchestrationHil(id, patch) issues a PATCH to /api/orchestration/hil/{id}/edit
    - It returns the parsed {ok, editedFields} on success
    - A 422 from the schema validator surfaces as a typed validation error, not a generic failure
  </behavior>
  <action>Add `editOrchestrationHil(id, patch)` to client.ts, mirroring the existing `resolveOrchestrationHil` fetch pattern. It PATCHes `/api/orchestration/hil/{id}/edit` with the patch body and returns the parsed `{ok, editedFields}`. Map a 422 response to a distinct typed validation-error result so the UI can show field-level rejection. Confirm the request/response shape against 70-02-SUMMARY.md before finalizing the type. Run gitnexus_impact if you modify any existing exported symbol in client.ts.</action>
  <verify>
    <automated>cd apps/memroos && npx vitest run src/lib/orchestration/__tests__/client.test.ts</automated>
  </verify>
  <done>client.test.ts passes; editOrchestrationHil PATCHes the edit route, returns {ok, editedFields}, and surfaces 422 as a typed validation error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auth-guarded PATCH /api/orchestration/hil/{id}/edit proxy route</name>
  <read_first>
    - apps/memroos/src/app/api/orchestration/hil/route.ts — authorizeRegistryWrite + registryWriteUnauthorizedResponse exact pattern
    - apps/memroos/src/app/api/orchestration/hil/[id]/route.ts — existing [id] route handler shape
    - RESEARCH.md Pitfall 5 (CC-5) — the auth guard is mandatory; node_modules/next/dist/docs/ per AGENTS.md for the route handler API
  </read_first>
  <files>apps/memroos/src/app/api/orchestration/hil/[id]/edit/route.ts</files>
  <behavior>
    - PATCH /api/orchestration/hil/{id}/edit returns 403 via registryWriteUnauthorizedResponse when authorizeRegistryWrite is false
    - When authorized, it proxies the patch body to the Python service PATCH /hil/{id}/edit and returns the upstream status and body
    - A 422 from the Python service is passed through unchanged so the validation error reaches the client
  </behavior>
  <action>Create `apps/memroos/src/app/api/orchestration/hil/[id]/edit/route.ts` exporting a `PATCH` handler. First call `authorizeRegistryWrite(request)`; if false, return `registryWriteUnauthorizedResponse()` — copy the exact guard pattern from the existing hil route (RESEARCH.md Pitfall 5 — mandatory, omitting it opens an unauthenticated tunnel-reachable endpoint). When authorized, proxy the request body to the Python orchestration service `PATCH /hil/{id}/edit` (use the ORCHESTRATION_SERVICE_URL env var), and return the upstream status and JSON unchanged so a 422 passes through. Read `node_modules/next/dist/docs/` for the current route-handler API before writing (AGENTS.md — this Next.js may differ from training data). No `execSync`/`exec`.</action>
  <verify>
    <automated>cd apps/memroos && npx vitest run "src/app/api/orchestration/hil/[id]/edit/__tests__/route.test.ts"</automated>
  </verify>
  <done>route.test.ts passes; unauthorized requests get 403; authorized requests proxy to the Python edit endpoint; upstream 422 is passed through unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: HilEditPanel operator edit UI</name>
  <read_first>
    - apps/memroos/src/lib/orchestration/client.ts — editOrchestrationHil (from Task 1)
    - existing orchestration HIL UI components (the approve/reject panel) for styling and layout conventions
    - node_modules/next/dist/docs/ per AGENTS.md before writing any Next.js/React code
  </read_first>
  <files>apps/memroos/src/components/orchestration/HilEditPanel.tsx</files>
  <action>Create `HilEditPanel.tsx`: a panel rendered for a paused (waiting_for_approval) HIL task that shows the task's editable OrchestrationState fields as form inputs pre-filled with current values. On submit it calls `editOrchestrationHil(id, patch)` sending only changed fields. On a 422 validation result, show field-level rejection messages and do not clear the form. On success, show the confirmed editedFields and indicate the operator can now resume. Match the existing HIL approve/reject component's styling. Read `node_modules/next/dist/docs/` first per AGENTS.md.</action>
  <verify>
    <human-check>Open a paused HIL task in the Memroos UI, edit a declared state field in HilEditPanel, submit. Confirm: (1) a valid edit shows the confirmed editedFields, (2) an edit with an invalid value shows a field-level error and keeps the form, (3) after a valid edit, resuming the task dispatches (does not terminate at END).</human-check>
  </verify>
  <done>HilEditPanel renders editable fields for a paused HIL task, submits via editOrchestrationHil, surfaces 422 field errors, and confirms accepted edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator browser → Next.js edit route | Untrusted client request crosses into the orchestration edit path |
| Next.js edit route → Python orchestration service | Proxied edit crosses the process boundary over HTTP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-14 | Elevation of Privilege | PATCH /api/orchestration/hil/{id}/edit | mitigate | authorizeRegistryWrite guard at the route handler; registryWriteUnauthorizedResponse returns 403 — exact pattern from the existing hil route (RESEARCH.md Pitfall 5) |
| T-70-15 | Tampering | edit patch body | mitigate | Patch is proxied unchanged to the Python HilEditRequest validator (Plan 02); unknown keys rejected with 422 server-side before update_state |
| T-70-16 | Information Disclosure | orchestration service URL | accept | ORCHESTRATION_SERVICE_URL is an existing env-configured internal address; no new secret introduced (RESEARCH.md Runtime State Inventory — no env renames) |
</threat_model>

<verification>
- `cd apps/memroos && npx vitest run src/lib/orchestration src/app/api/orchestration/hil` — client + route tests green.
- `cd apps/memroos && npx vitest run` — full TS suite green (per-wave merge gate).
- Manual: HilEditPanel edit → resume dispatches (human-check in Task 3).
</verification>

<success_criteria>
- An operator can edit a paused HIL task's declared state fields via HilEditPanel and submit (HIL-01).
- The TS edit route enforces authorizeRegistryWrite and proxies the validated patch; 422 schema rejection surfaces to the operator (HIL-02).
- Accepted edits are audited end-to-end (HIL-03 audit row written by Plan 02; this plan completes the operator-facing path that produces the actor identity).
</success_criteria>

<output>
Create `.planning/phases/70-foundation-engine-core/70-05-SUMMARY.md` when done.
</output>
