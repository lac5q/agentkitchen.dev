---
phase: 70-foundation-engine-core
name: Foundation Prerequisites — WAL pragma + langgraph pin + Wave 0 test scaffolds
plan: "01"
type: execute
autonomous: true
wave: 1
depends_on: []
files_modified:
  - services/orchestration/engine.py
  - services/orchestration/requirements.txt
  - services/orchestration/tests/test_graph_runtime.py
  - services/orchestration/tests/test_engine.py
  - services/orchestration/tests/test_app.py
  - apps/memroos/src/lib/memory/__tests__/registry.test.ts
  - apps/memroos/src/lib/memory/__tests__/adapters.test.ts
  - apps/memroos/src/components/orchestration/__tests__/HilEditPanel.test.tsx
requirements:
  - HIL-01
  - HIL-02
  - HIL-03
  - ORCH-08
  - ORCH-09
  - ORCH-10
  - MEM-06
  - MEM-07
  - MEM-08
requirements_addressed:
  - HIL-01
  - HIL-02
  - HIL-03
  - ORCH-08
  - ORCH-09
  - ORCH-10
  - MEM-06
  - MEM-07
  - MEM-08

must_haves:
  truths:
    - "orchestration.db opens in WAL journal mode with a 5000ms busy timeout"
    - "langgraph is pinned to >=1.2,<2.0 in the orchestration service requirements"
    - "Every Phase 70 requirement has a failing (RED) automated test that exists before implementation begins"
  artifacts:
    - path: "services/orchestration/engine.py"
      provides: "WAL pragma + busy_timeout in OrchestrationStore.__init__ before _init_schema()"
      contains: "journal_mode=WAL"
    - path: "services/orchestration/requirements.txt"
      provides: "langgraph version pin"
      contains: "langgraph>=1.2,<2.0"
    - path: "apps/memroos/src/lib/memory/__tests__/registry.test.ts"
      provides: "Wave 0 scaffold for MEM-07 adapter registry"
    - path: "apps/memroos/src/lib/memory/__tests__/adapters.test.ts"
      provides: "Wave 0 scaffold for MEM-06/MEM-08 concrete adapter contracts"
  key_links:
    - from: "OrchestrationStore.__init__"
      to: "PRAGMA journal_mode=WAL"
      via: "conn.execute before _init_schema"
      pattern: "journal_mode=WAL"
---

<objective>
Land the three hard prerequisites that gate every other Phase 70 plan, and create the Wave 0 test scaffolds (RED tests) for all nine Phase 70 requirements.

This plan carries all nine Phase 70 requirement IDs because its work is a hard precondition for each of them: the WAL pragma prevents concurrent edit+resume stalls that HIL-01..03 would trigger, the `langgraph>=1.2,<2.0` pin unlocks `RetryPolicy` required by ORCH-08..10, and the Wave 0 test scaffolds define the verifiable contracts for MEM-06..08 and every other requirement. No requirement below can be implemented or verified until this plan lands.

Purpose: Without WAL on `orchestration.db`, concurrent HIL edit + resume stalls under the rollback journal (RESEARCH.md, Environment Availability — "no fallback"). Without the langgraph pin, `RetryPolicy` may be unavailable. Without RED test scaffolds, downstream plans have no Nyquist-compliant `<automated>` verification target.
Output: WAL-enabled orchestration store, pinned langgraph, and five test files containing failing tests for all nine requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-foundation-engine-core/70-RESEARCH.md
@.planning/phases/70-foundation-engine-core/70-CONTEXT.md
@CLAUDE.md
@services/orchestration/engine.py
@services/orchestration/requirements.txt
@apps/memroos/src/lib/memory/backends.ts
@apps/memroos/src/lib/memory/tiers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WAL pragma + busy_timeout to OrchestrationStore</name>
  <read_first>
    - services/orchestration/engine.py — OrchestrationStore.__init__ (currently calls executescript with no PRAGMA)
    - apps/memroos/src/lib/db.ts — reference WAL pragma pattern already used on memroos.db
    - RESEARCH.md Pattern 1 — exact placement: pragma must run BEFORE self._init_schema()
  </read_first>
  <files>services/orchestration/engine.py</files>
  <action>In OrchestrationStore.__init__, after the sqlite3.connect() call and row_factory assignment but BEFORE the self._init_schema() call, add two pragma statements: `self.conn.execute("PRAGMA journal_mode=WAL")` and `self.conn.execute("PRAGMA busy_timeout=5000")`. Placement before _init_schema() is mandatory — executescript() commits pending transactions, so the pragma must be active from the first write (RESEARCH.md Pattern 1). Do NOT open orchestration.db from any Next.js process — the two-DB boundary is inviolable (CLAUDE.md / RESEARCH.md Project Constraints). Run gitnexus_impact on OrchestrationStore.__init__ before editing and report blast radius.</action>
  <verify>
    <automated>cd services/orchestration && python -c "from engine import OrchestrationStore; import tempfile, os; p=os.path.join(tempfile.mkdtemp(),'t.db'); s=OrchestrationStore(p); print(s.conn.execute('PRAGMA journal_mode').fetchone()[0])" | grep -qi wal && echo PASS</automated>
  </verify>
  <done>Instantiating OrchestrationStore reports journal_mode=wal; busy_timeout is 5000; pragma statements appear before _init_schema() in __init__.</done>
</task>

<task type="auto">
  <name>Task 2: Pin langgraph and verify compatibility</name>
  <read_first>
    - services/orchestration/requirements.txt — langgraph line is currently unpinned
    - RESEARCH.md Open Question 1 — run a dry-run install before changing the file to verify Python 3.14 compatibility
  </read_first>
  <files>services/orchestration/requirements.txt</files>
  <action>First run `python -m pip install "langgraph>=1.2,<2.0" --dry-run` (use the orchestration venv if one is active) to confirm a compatible version resolves on the installed Python. If it resolves, change the unpinned `langgraph` line in requirements.txt to `langgraph>=1.2,<2.0`. If the dry-run fails to resolve a 1.2+ version, stop and record the failure in the SUMMARY as a blocker — do not proceed to downstream plans. RetryPolicy (needed by ORCH-08) requires langgraph >= 1.2 (RESEARCH.md Standard Stack).</action>
  <verify>
    <automated>grep -qE '^langgraph>=1\.2,<2\.0' services/orchestration/requirements.txt && echo PASS || echo FAIL</automated>
  </verify>
  <done>requirements.txt contains `langgraph>=1.2,<2.0`; dry-run resolution confirmed; any incompatibility recorded as an explicit SUMMARY blocker.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 RED test scaffolds for all nine requirements</name>
  <read_first>
    - services/orchestration/tests/test_graph_runtime.py, test_engine.py, test_app.py — existing test files to extend (do not overwrite)
    - apps/memroos/src/lib/memory/__tests__/tiers.test.ts — reference vitest style for new memory test files
    - RESEARCH.md "Phase Requirements -> Test Map" — exact test names and commands per requirement
  </read_first>
  <files>services/orchestration/tests/test_graph_runtime.py, services/orchestration/tests/test_engine.py, services/orchestration/tests/test_app.py, apps/memroos/src/lib/memory/__tests__/registry.test.ts, apps/memroos/src/lib/memory/__tests__/adapters.test.ts, apps/memroos/src/components/orchestration/__tests__/HilEditPanel.test.tsx</files>
  <action>Add failing (RED) tests, one per requirement, using the names from RESEARCH.md Test Map. Python: extend test_graph_runtime.py with `test_..._edit` (HIL-01) and `test_..._retry_policy` (ORCH-08); extend test_app.py with `test_..._edit_validation` (HIL-02); extend test_engine.py with `test_..._state_edit_audit` (HIL-03), `test_..._compensation_row` (ORCH-09), `test_..._rolled_back_status` (ORCH-10). TypeScript: create registry.test.ts (MEM-07 — registerAdapter/getAdapters), adapters.test.ts (MEM-06 no client-handle leakage, MEM-08 concrete adapter search/write/health contracts), and HilEditPanel.test.tsx (HIL-01..03 UI Wave 0 scaffold — renders with a paused task, calls editOrchestrationHil on submit). Each test must assert the real target behavior so it fails now and passes only when the downstream plan implements it — do NOT write trivially-passing placeholders. Mark each test with the requirement ID in a comment.</action>
  <verify>
    <automated>cd services/orchestration && [ "$(python -m pytest tests/ -k 'edit or retry_policy or state_edit_audit or compensation_row or rolled_back_status' --co -q | grep -c 'test_')" -ge 6 ] && echo PYTHON_PASS || echo PYTHON_FAIL; cd ../.. && test -f apps/memroos/src/lib/memory/__tests__/registry.test.ts && test -f apps/memroos/src/lib/memory/__tests__/adapters.test.ts && test -f apps/memroos/src/components/orchestration/__tests__/HilEditPanel.test.tsx && echo TS_FILES_PASS || echo TS_FILES_FAIL</automated>
  </verify>
  <done>Six test files contain RED tests covering all nine requirements + HilEditPanel; Python scaffolds are collectable by pytest; all three TS test files exist on disk and fail until implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python process ↔ orchestration.db | SQLite file accessed only by the Python service; no cross-process open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Tampering | orchestration.db concurrent writers | mitigate | WAL + busy_timeout=5000 serializes concurrent edit+resume safely; two-DB boundary keeps Next.js out of the file |
| T-70-SC | Tampering | langgraph pip pin | mitigate | Pin to a bounded range `>=1.2,<2.0`; langgraph is an existing approved dependency (RESEARCH.md Package Legitimacy Audit — no net-new packages, no `[ASSUMED]`/`[SUS]` items) |
</threat_model>

<verification>
- `cd services/orchestration && python -c "..."` confirms journal_mode=wal on a fresh OrchestrationStore.
- `grep -E '^langgraph>=1\.2,<2\.0' services/orchestration/requirements.txt` confirms the pin.
- `cd services/orchestration && python -m pytest tests/ --co -q` collects the new RED scaffolds.
- `cd apps/memroos && npx vitest run src/lib/memory` shows the two new test files failing (RED) — expected until Plan 04 lands.
- Run `gitnexus_detect_changes()` before committing — verify scope matches expected WAL/pin/scaffold files only.
</verification>

<success_criteria>
- OrchestrationStore opens orchestration.db in WAL mode with busy_timeout=5000, pragma set before _init_schema().
- requirements.txt pins `langgraph>=1.2,<2.0`; compatibility dry-run confirmed.
- All nine Phase 70 requirements have a named, failing automated test in one of the five scaffold files.
</success_criteria>

<output>
Create `.planning/phases/70-foundation-engine-core/70-01-SUMMARY.md` when done.
</output>
