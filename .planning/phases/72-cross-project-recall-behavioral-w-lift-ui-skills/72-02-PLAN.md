---
phase: 72-cross-project-recall-behavioral-w-lift-ui-skills
name: Behavioral eval job substrate
plan: "02"
type: execute
autonomous: false
wave: 1
depends_on: ["70-04"]
files_modified:
  - apps/memroos/src/lib/db-schema.ts
  - apps/memroos/src/lib/seal/behavioral-jobs.ts
  - apps/memroos/src/lib/seal/behavioral-sandbox.ts
  - apps/memroos/src/lib/seal/service.ts
  - apps/memroos/src/lib/seal/__tests__/behavioral-jobs.test.ts
  - apps/memroos/src/lib/seal/__tests__/seal-substrate.test.ts
requirements:
  - SEAL-04
  - SEAL-05
  - SEAL-06
must_haves:
  truths:
    - "Behavioral proposal classes enqueue a durable job instead of blocking"
    - "Sandbox profile replaces side-effect tools with no-op recording stubs"
    - "Legacy synchronous proposal classes keep working"
    - "Evidence bundle fields are persisted, even when some values are missing"
---

<objective>
Create the durable async job and sandbox foundation for true behavioral W-lift
without destabilizing existing SEAL proposal lifecycle behavior.
</objective>

<context>
@.planning/phases/72-cross-project-recall-behavioral-w-lift-ui-skills/72-CONTEXT.md
@.planning/phases/72-cross-project-recall-behavioral-w-lift-ui-skills/72-RESEARCH.md
@apps/memroos/src/lib/seal/service.ts
@apps/memroos/src/lib/evals/service.ts
@apps/memroos/src/lib/db-schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for job state machine and sandbox contract</name>
  <action>Add tests for creating queued/running/passed/failed/rolled_back jobs, persisting evidence bundles, and recording no-op tool calls without external side effects.</action>
  <verify>cd apps/memroos && npx vitest run src/lib/seal/__tests__/behavioral-jobs.test.ts</verify>
</task>

<task type="auto">
  <name>Task 2: Additive job/evidence schema</name>
  <action>Add `seal_eval_jobs` and evidence persistence through `initSchema()` using guarded additive migrations. Run GitNexus impact for `initSchema` first.</action>
  <verify>cd apps/memroos && npx vitest run src/lib/seal/__tests__/behavioral-jobs.test.ts</verify>
</task>

<task type="auto">
  <name>Task 3: Async apply path for behavioral proposal classes</name>
  <action>Teach `SealService.applyProposal()` to return a `jobId` ApplyResult variant for behavioral proposal classes while keeping legacy synchronous proposal types unchanged.</action>
  <verify>cd apps/memroos && npx vitest run src/lib/seal/__tests__/seal-substrate.test.ts src/lib/seal/__tests__/behavioral-jobs.test.ts && npm run typecheck</verify>
</task>

</tasks>

<risks>
- `initSchema` and `SealService.applyProposal` are high-blast-radius; run GitNexus impact before edits.
- Sandbox must fail closed: no live side-effecting tool calls during behavioral eval.
</risks>

<done>
Behavioral proposals enqueue jobs, sandbox contracts are test-covered, evidence bundles persist, and existing SEAL proposal behavior remains green.
</done>
