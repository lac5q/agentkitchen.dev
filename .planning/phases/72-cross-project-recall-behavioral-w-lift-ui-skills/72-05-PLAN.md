---
phase: 72-cross-project-recall-behavioral-w-lift-ui-skills
name: Cross-harness skill registry and import UI
plan: "05"
type: execute
autonomous: false
wave: 1
depends_on: []
files_modified:
  - apps/memroos/src/lib/db-schema.ts
  - apps/memroos/src/lib/skills/registry.ts
  - apps/memroos/src/app/api/skills/import/route.ts
  - apps/memroos/src/app/api/skills/route.ts
  - apps/memroos/src/app/skills/page.tsx
  - apps/memroos/src/lib/skills/__tests__/registry.test.ts
requirements:
  - SKILL-01
  - SKILL-02
  - SKILL-04
must_haves:
  truths:
    - "skill_registry stores normalized SKILL.md contracts"
    - "Manual import normalizes source harness and governed contract fields"
    - "Skills UI shows source harness, risk tier, dispatch status, owner, verification checks, and completeness"
---

<objective>
Create the governed skill registry and manual import surface for cross-harness
SKILL.md contracts.
</objective>

<context>
@.planning/phases/72-cross-project-recall-behavioral-w-lift-ui-skills/72-CONTEXT.md
@.planning/phases/72-cross-project-recall-behavioral-w-lift-ui-skills/72-RESEARCH.md
@apps/memroos/src/app/api/skills/route.ts
@apps/memroos/src/app/skills/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registry parser/completeness tests</name>
  <action>Add tests for SKILL.md metadata normalization, required contract fields, completeness scoring, and prompt-injection-as-data handling.</action>
  <verify>cd apps/memroos && npx vitest run src/lib/skills/__tests__/registry.test.ts</verify>
</task>

<task type="auto">
  <name>Task 2: Additive schema and import API</name>
  <action>Add `skill_registry` schema and authenticated manual import route. Treat imported content as data only; do not execute skill text.</action>
  <verify>cd apps/memroos && npx vitest run src/lib/skills/__tests__/registry.test.ts && npm run typecheck</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Skills UI contract completeness</name>
  <action>Extend Skills UI to show governed/imported skills separately from discovered filesystem/log skills, including completeness and missing-field states.</action>
  <verify>cd apps/memroos && npm run build; browser smoke `/skills` import and completeness states</verify>
</task>

</tasks>

<risks>
- Imported SKILL.md can contain hostile instructions; never treat it as instruction during normalization.
- `initSchema` requires GitNexus impact before editing.
</risks>

<done>
SKILL-01, SKILL-02, and SKILL-04 are implemented with tests and visible UI evidence.
</done>
