---
phase: 72-cross-project-recall-behavioral-w-lift-ui-skills
name: Cross-project recall contract
plan: "01"
type: execute
autonomous: true
wave: 1
depends_on: ["71-02"]
files_modified:
  - apps/memroos/src/app/api/recall/route.ts
  - apps/memroos/src/lib/embeddings/recall.ts
  - apps/memroos/src/lib/embeddings/__tests__/recall.test.ts
  - apps/memroos/src/app/api/recall/__tests__/route.test.ts
requirements:
  - RECALL-03
  - RECALL-04
must_haves:
  truths:
    - "Single-project recall remains the default"
    - "Cross-project recall requires crossProject=true and explicit allowed_project_ids"
    - "Cross-project results include source project/repo annotation"
    - "Semantic/hybrid degraded fallback remains HTTP 200 with degraded:true"
---

<objective>
Extend recall with an explicit cross-project contract while preserving Phase 71
single-project defaults and degraded fallback behavior.
</objective>

<context>
@.planning/phases/72-cross-project-recall-behavioral-w-lift-ui-skills/72-CONTEXT.md
@.planning/phases/72-cross-project-recall-behavioral-w-lift-ui-skills/72-RESEARCH.md
@apps/memroos/src/app/api/recall/route.ts
@apps/memroos/src/lib/embeddings/recall.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for recall scope</name>
  <action>Add route and utility tests proving default single-project behavior, explicit allowlist behavior, empty/unauthorized allowlist behavior, source annotations, and degraded fallback.</action>
  <verify>cd apps/memroos && npx vitest run src/lib/embeddings/__tests__/recall.test.ts src/app/api/recall/__tests__/route.test.ts</verify>
</task>

<task type="auto">
  <name>Task 2: Scope-aware recall ranking</name>
  <action>Extend semantic/hybrid recall helpers or route-level filtering so allowed project IDs constrain candidate rows before final ranking and every returned row has source project/repo metadata.</action>
  <verify>cd apps/memroos && npx vitest run src/lib/embeddings/__tests__/recall.test.ts</verify>
</task>

<task type="auto">
  <name>Task 3: API contract and side effects</name>
  <action>Update `/api/recall` to parse crossProject/allowed_project_ids, keep GET backward compatible, preserve recall side effects, and return structured errors only for invalid explicit cross-project requests.</action>
  <verify>cd apps/memroos && npx vitest run src/app/api/recall/__tests__/route.test.ts && npm run typecheck</verify>
</task>

</tasks>

<risks>
- Run GitNexus API impact for `/api/recall` before editing; consumers read `results`.
- Do not add recursive filesystem scanning or broad repo discovery.
</risks>

<done>
RECALL-03 and RECALL-04 are covered by tests; default recall is unchanged; cross-project recall is explicit, ranked, annotated, and degraded-safe.
</done>
