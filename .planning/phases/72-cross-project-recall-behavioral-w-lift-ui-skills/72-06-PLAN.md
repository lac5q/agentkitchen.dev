---
phase: 72-cross-project-recall-behavioral-w-lift-ui-skills
name: Skill-aware dispatch and evidence integration
plan: "06"
type: execute
autonomous: false
wave: 2
depends_on: ["72-05"]
files_modified:
  - apps/memroos/src/lib/dispatch/a2a-adapter.ts
  - apps/memroos/src/lib/dispatch/types.ts
  - apps/memroos/src/lib/skills/registry.ts
  - apps/memroos/src/app/api/dispatch/route.ts
  - apps/memroos/src/lib/dispatch/__tests__/skill-dispatch.test.ts
requirements:
  - SKILL-03
must_haves:
  truths:
    - "A2A dispatcher checks skill_registry before per-agent instruction fallback"
    - "Incomplete, disabled, or risk-unknown skill contracts fail closed"
    - "Dispatch evidence records skill selection or denial reason"
---

<objective>
Make governed skill contracts affect dispatch and produce auditable evidence.
</objective>

<context>
@.planning/phases/72-cross-project-recall-behavioral-w-lift-ui-skills/72-CONTEXT.md
@.planning/phases/72-cross-project-recall-behavioral-w-lift-ui-skills/72-RESEARCH.md
@apps/memroos/src/lib/dispatch/a2a-adapter.ts
@apps/memroos/src/app/api/dispatch/route.ts
@apps/memroos/src/lib/skills/registry.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skill dispatch tests</name>
  <action>Add tests for registry hit, fallback, disabled contract denial, incomplete contract denial, and evidence shape.</action>
  <verify>cd apps/memroos && npx vitest run src/lib/dispatch/__tests__/skill-dispatch.test.ts</verify>
</task>

<task type="auto">
  <name>Task 2: Dispatcher registry lookup</name>
  <action>Update dispatch path to look up enabled complete skill contracts before per-agent instruction fallback. Keep existing dispatch behavior when no governed skill applies.</action>
  <verify>cd apps/memroos && npx vitest run src/lib/dispatch/__tests__/skill-dispatch.test.ts && npm run typecheck</verify>
</task>

<task type="auto">
  <name>Task 3: Evidence integration</name>
  <action>Extend dispatch evidence to include selected skill ID/source harness/risk tier or denial reason, and make API errors visible to callers.</action>
  <verify>cd apps/memroos && npx vitest run src/lib/dispatch && npm run build</verify>
</task>

</tasks>

<risks>
- Dispatch must fail closed for incomplete governed contracts without breaking agents that have no matching registry entry.
- Evidence must avoid leaking raw secrets or unsafe imported instructions.
</risks>

<done>
SKILL-03 is implemented and dispatch evidence proves the selected governed skill or denial reason.
</done>
