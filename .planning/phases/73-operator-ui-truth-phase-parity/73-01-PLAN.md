---
phase: 73-operator-ui-truth-phase-parity
name: Operator UI truth and phase parity
plan: "01"
type: execute
autonomous: false
depends_on: ["72-06"]
files_modified:
  - apps/memroos/src/components/orchestration/orchestration-hil-panel.tsx
  - apps/memroos/src/components/operations/noc-header.tsx
  - apps/memroos/src/components/operations/efficiency-signals.tsx
  - apps/memroos/src/lib/gsd/phase-ui-representation.ts
  - .planning/GOAL.md
requirements:
  - UI-PARITY-01
  - UI-PARITY-02
  - UI-PARITY-03
  - UI-PARITY-04
  - UI-PARITY-05
must_haves:
  truths:
    - "Completed phase claims match visible operator behavior and planning files"
    - "Phase 70 HIL edit-and-continue is reachable from the live approval panel"
    - "The Operations NOC does not present unimplemented efficiency telemetry as live numbers"
    - "Future phase summaries declare visible UI, existing UI provenance, API-only scope, or follow-up UI debt"
---

# Phase 73 Plan 01: Operator UI Truth And Phase Parity

<objective>
Close the parity gap between backend-complete phase claims and operator-visible product truth.
</objective>

<context>
@.planning/phases/73-operator-ui-truth-phase-parity/73-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/GOAL.md
@apps/memroos/src/components/orchestration/orchestration-hil-panel.tsx
@apps/memroos/src/components/orchestration/HilEditPanel.tsx
@apps/memroos/src/components/operations/index.tsx
@apps/memroos/src/components/operations/noc-header.tsx
@apps/memroos/src/components/operations/efficiency-signals.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire HIL edit UI into live approvals</name>
  <action>Mount `HilEditPanel` inside `OrchestrationHilPanel` so pending HIL tasks can be edited from the visible approval surface before approval/resume.</action>
  <verify>cd apps/memroos && npx vitest run src/components/orchestration/__tests__/hil-panel.test.tsx src/components/orchestration/__tests__/HilEditPanel.test.tsx</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make NOC telemetry truth visible</name>
  <action>Replace the efficiency sample metrics with a missing-telemetry checklist, and change the NOC header from live/fresh claims to explicit telemetry-preview language while live wiring remains pending.</action>
  <verify>cd apps/memroos && npx vitest run src/components/operations/__tests__/efficiency-signals.test.tsx src/components/operations/__tests__/operations-noc.test.tsx</verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add phase-close UI representation gate</name>
  <action>Add a small validator and update the phase done definition so every completed requirement declares visible UI, existing UI provenance, API-only scope, or a follow-up UI requirement.</action>
  <verify>cd apps/memroos && npx vitest run src/lib/gsd/__tests__/phase-ui-representation.test.ts</verify>
</task>

<task type="auto">
  <name>Task 4: Reconcile planning truth</name>
  <action>Update requirements, roadmap, state, and this phase summary so Phase 73 is represented as the parity closeout for v4.0.</action>
  <verify>npm run typecheck && npm run build && authenticated browser smoke for `/` and `/flow`</verify>
</task>

</tasks>

<risks>
- The NOC still has broader real-data work tracked in NOC-01..11; this phase must not overclaim that those panels are live.
- HIL edit UI must remain additive to approval/rejection behavior and preserve existing empty/error states.
- Phase-close gating starts as a local validator plus planning contract; broader GSD CLI enforcement can follow when the workflow code is externalized.
</risks>

<done>
UI-PARITY-01..05 are satisfied for the v4.0 parity closeout, with broader real-data NOC wiring still tracked under NOC-01..11.
</done>
