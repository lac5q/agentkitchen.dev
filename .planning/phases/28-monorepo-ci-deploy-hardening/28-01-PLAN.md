# Phase 28 Plan: Monorepo CI and Deploy Hardening

## Objective

Finish the monorepo by adding CI coverage that matches the new layout and documenting the deployment handoff.

## Scope

- Add a GitHub Actions workflow for Kitchen tests/build and Python service smoke tests.
- Keep Secret Guard as the security gate.
- Confirm local production deployment after the monorepo merge.
- Update GSD planning state so v1.6 is no longer marked TBD.

## Tasks

1. Add `.github/workflows/ci.yml`.
2. Run Kitchen tests and build locally.
3. Run Knowledge MCP tests locally.
4. Run memory and shell script smoke checks.
5. Restart the production LaunchAgent.
6. Update v1.6 roadmap, requirements, state, and milestone records.

## Validation

- `npm --prefix apps/kitchen run test -- --run`
- `npm run build`
- `PYTHONPATH=services/knowledge-mcp python -m pytest services/knowledge-mcp/tests`
- `python -m py_compile services/memory/*.py services/knowledge-mcp/knowledge_system/*.py`
- `bash -n start.sh services/memory/*.sh`
- Live `GET /api/health`
