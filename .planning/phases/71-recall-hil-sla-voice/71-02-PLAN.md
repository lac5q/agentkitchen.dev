---
phase: 71-recall-hil-sla-voice
name: Semantic/hybrid recall endpoint + background embedding job
plan: "02"
type: execute
autonomous: true
wave: 2
depends_on: ["71-01", "71-03"]
files_modified:
  - apps/memroos/src/app/api/recall/route.ts
  - apps/memroos/src/lib/embeddings/recall.ts
  - apps/memroos/src/lib/embeddings/embedding-job.ts
  - apps/memroos/src/instrumentation.ts
  - apps/memroos/src/lib/embeddings/__tests__/recall.test.ts
  - apps/memroos/src/lib/embeddings/__tests__/embedding-job.test.ts
requirements:
  - RECALL-01
  - RECALL-02
requirements_addressed:
  - RECALL-01
  - RECALL-02
must_haves:
  truths:
    - "GET /api/recall accepts mode=semantic|bm25|hybrid; absent/unknown mode uses BM25"
    - "Hybrid mode merges BM25 + semantic results via Reciprocal Rank Fusion"
    - "An embedding outage returns results with degraded:true and HTTP 200, never a 5xx"
    - "A background job embeds up to 50 un-embedded messages every 5 minutes"
  artifacts:
    - path: "apps/memroos/src/lib/embeddings/recall.ts"
      provides: "semanticRecall + hybridRecall (RRF) over message_embeddings"
      contains: "RRF_K"
    - path: "apps/memroos/src/lib/embeddings/embedding-job.ts"
      provides: "startEmbeddingJob — 50 msgs/cycle, 5-min interval"
      contains: "EMBEDDING_CYCLE_LIMIT"
    - path: "apps/memroos/src/app/api/recall/route.ts"
      provides: "mode-aware recall endpoint with degraded flag"
      contains: "degraded"
  key_links:
    - from: "apps/memroos/src/app/api/recall/route.ts"
      to: "apps/memroos/src/lib/embeddings/recall.ts"
      via: "import semanticRecall/hybridRecall by mode"
      pattern: "semanticRecall|hybridRecall"
    - from: "apps/memroos/src/instrumentation.ts"
      to: "apps/memroos/src/lib/embeddings/embedding-job.ts"
      via: "startEmbeddingJob() inside scheduler-singleton guard"
      pattern: "startEmbeddingJob"
---

<objective>
Wire LLM semantic recall into `GET /api/recall` and ship the background job that
precomputes message embeddings.

Purpose: RECALL-01 requires `mode=semantic|bm25|hybrid` on the recall endpoint
with RRF hybrid fusion, BM25 default, and graceful degradation. RECALL-02
requires embeddings precomputed at ingest by a background job (50 messages per
cycle, 5-minute interval). This plan depends on 71-01 for the
`message_embeddings` table, the embedding provider, and the store helpers.

Output: `embeddings/recall.ts` (semantic + RRF hybrid), `embeddings/embedding-job.ts`
(background job), the upgraded recall route, and `instrumentation.ts` registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-recall-hil-sla-voice/71-CONTEXT.md
@.planning/phases/71-recall-hil-sla-voice/71-01-SUMMARY.md
@CLAUDE.md
@apps/memroos/src/app/api/recall/route.ts
@apps/memroos/src/instrumentation.ts
@apps/memroos/src/lib/db-ingest.ts

<interfaces>
Contracts delivered by 71-01 — use these directly, no exploration needed.

From apps/memroos/src/lib/embeddings/provider.ts (71-01):
- type EmbedResult = { embedding: number[]; degraded: false } | { embedding: null; degraded: true }
- function embeddingProviderEnabled(): boolean
- async function embedText(text: string): Promise<EmbedResult>
- function cosineSimilarity(a: number[], b: number[]): number

From apps/memroos/src/lib/embeddings/store.ts (71-01):
- function upsertEmbedding(db, messageId: number, vector: number[], model: string): void
- function getEmbedding(db, messageId: number): number[] | null
- function messagesNeedingEmbedding(db, limit: number): number[]

Existing BM25 path — apps/memroos/src/lib/db-ingest.ts:
- recallByKeyword(db, query, limit) returns rows with id, session_id, project,
  agent_id, role, snippet, timestamp, rank.

instrumentation.ts already guards schedulers with tryAcquireSchedulerLock() and
calls startConsolidationScheduler() / startDecayScheduler(). Add
startEmbeddingJob() inside the same NEXT_RUNTIME === 'nodejs' + lock block.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 RED scaffolds — recall + embedding-job tests</name>
  <read_first>
    - apps/memroos/src/lib/embeddings/__tests__/store.test.ts — DB setup/teardown pattern from 71-01
  </read_first>
  <files>apps/memroos/src/lib/embeddings/__tests__/recall.test.ts, apps/memroos/src/lib/embeddings/__tests__/embedding-job.test.ts</files>
  <action>
    Create two failing vitest suites.

    recall.test.ts — seed a fresh DB with a few `messages` rows and matching
    `message_embeddings` rows via 71-01 `upsertEmbedding`. Assert: (1)
    `semanticRecall(db, queryVector, limit)` ranks messages by cosine similarity
    descending; (2) `hybridRecall(db, query, queryVector, limit)` produces an
    RRF-merged list — a message appearing in both the BM25 list and the semantic
    list outranks one appearing in only one (mock `recallByKeyword` to control
    the BM25 side); (3) RRF uses `k = 60` (assert exported `RRF_K`).

    embedding-job.test.ts — assert: (1) `runEmbeddingCycle(db)` embeds at most
    50 messages (mock `embedText` to return a fixed vector; seed >50 un-embedded
    messages; assert exactly 50 `message_embeddings` rows after one cycle); (2)
    when `embedText` returns `degraded: true`, the cycle writes no rows and does
    not throw; (3) `EMBEDDING_INTERVAL_MS` equals 300000 and
    `EMBEDDING_CYCLE_LIMIT` equals 50.

    Both suites MUST fail (modules do not exist yet).
  </action>
  <verify>
    <automated>cd apps/memroos && npx vitest run src/lib/embeddings/__tests__/recall.test.ts src/lib/embeddings/__tests__/embedding-job.test.ts 2>&1 | grep -Eq 'fail|FAIL|Cannot find module' && echo "RED OK"</automated>
  </verify>
  <done>Both test files exist and fail because recall.ts and embedding-job.ts do not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: semanticRecall + hybridRecall (RRF) module</name>
  <read_first>
    - apps/memroos/src/lib/db-ingest.ts — recallByKeyword return shape
    - apps/memroos/src/lib/embeddings/provider.ts — cosineSimilarity from 71-01
  </read_first>
  <files>apps/memroos/src/lib/embeddings/recall.ts</files>
  <behavior>
    - semanticRecall ranks by cosine similarity descending, capped at limit
    - hybridRecall RRF-merges BM25 + semantic; a message in both lists outranks one in only one
    - RRF constant RRF_K is 60 (D-04)
  </behavior>
  <action>
    Create `apps/memroos/src/lib/embeddings/recall.ts` exporting:
    - `export const RRF_K = 60`.
    - `semanticRecall(db, queryVector: number[], limit: number)` — load
      `message_embeddings` rows (reasonable cap), score each with
      `cosineSimilarity(queryVector, vector)`, join back to `messages` for
      content/metadata, return the top `limit` sorted by score descending. Reuse
      the recall row shape from `recallByKeyword` plus a `score` field.
    - `hybridRecall(db, query: string, queryVector: number[], limit: number)` —
      per D-04: run `recallByKeyword(db, query, limit*2)` and
      `semanticRecall(db, queryVector, limit*2)`, then merge with Reciprocal
      Rank Fusion: for each message id, `rrf = sum over lists of 1/(RRF_K + rank)`.
      Return the top `limit` by `rrf` descending, each annotated with the source
      list(s).
    Keep both functions pure (explicit `db` arg) so they are unit-testable.
  </action>
  <verify>
    <automated>cd apps/memroos && npx vitest run src/lib/embeddings/__tests__/recall.test.ts 2>&1 | grep -Eq 'pass|PASS' && echo "RECALL GREEN"</automated>
  </verify>
  <done>recall.test.ts passes; RRF merge is correct; type check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Background embedding job + instrumentation registration</name>
  <read_first>
    - apps/memroos/src/instrumentation.ts — scheduler-singleton lock + dynamic-import pattern
    - apps/memroos/src/lib/memory-decay.ts — existing setInterval scheduler shape
  </read_first>
  <files>apps/memroos/src/lib/embeddings/embedding-job.ts, apps/memroos/src/instrumentation.ts</files>
  <behavior>
    - runEmbeddingCycle embeds at most 50 un-embedded messages per call
    - a degraded embedText result writes no rows and does not throw
    - startEmbeddingJob schedules on a 300000 ms (5-minute) interval
  </behavior>
  <action>
    Per D-06: create `apps/memroos/src/lib/embeddings/embedding-job.ts` exporting:
    - `export const EMBEDDING_CYCLE_LIMIT = 50` and
      `export const EMBEDDING_INTERVAL_MS = 300_000`.
    - `async runEmbeddingCycle(db = getDb())` returning
      `{ embedded: number; degraded: boolean }` — call
      `messagesNeedingEmbedding(db, EMBEDDING_CYCLE_LIMIT)`, fetch each message's
      content, `embedText` it, and `upsertEmbedding` on success. If `embedText`
      returns `degraded: true`, stop the cycle early and return
      `{ embedded, degraded: true }`. Wrap the whole cycle so it never throws
      into the scheduler.
    - `startEmbeddingJob(): void` — if `embeddingProviderEnabled()` is false,
      log and return without scheduling (D-01: null provider = no job).
      Otherwise `setInterval(runEmbeddingCycle, EMBEDDING_INTERVAL_MS)` plus one
      immediate kick-off; mirror the structure of `startDecayScheduler`.

    Edit `instrumentation.ts`: inside the existing `NEXT_RUNTIME === 'nodejs'` +
    `tryAcquireSchedulerLock()` block, dynamically import and call
    `startEmbeddingJob()` alongside the existing schedulers. Run
    `gitnexus_impact` on the `register` function before editing.
  </action>
  <verify>
    <automated>cd apps/memroos && npx vitest run src/lib/embeddings/__tests__/embedding-job.test.ts 2>&1 | grep -Eq 'pass|PASS' && echo "JOB GREEN"</automated>
  </verify>
  <done>embedding-job.test.ts passes; instrumentation.ts registers startEmbeddingJob inside the scheduler lock; type check clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Mode-aware recall endpoint with degraded flag</name>
  <read_first>
    - apps/memroos/src/app/api/recall/route.ts — existing GET handler; preserve recall_log + memory_salience side effects
    - node_modules/next/dist/docs/ — route handler / Request API guide if unfamiliar
  </read_first>
  <files>apps/memroos/src/app/api/recall/route.ts</files>
  <behavior>
    - mode=bm25 (or absent/unknown) returns the existing recallByKeyword result
    - mode=semantic ranks by embedding similarity; mode=hybrid RRF-merges both
    - an embedding outage returns HTTP 200 with degraded:true and BM25 results
  </behavior>
  <action>
    Per D-03, D-04, D-05: extend `GET /api/recall` to read a `mode` query param
    (`semantic` | `bm25` | `hybrid`). Default and any unrecognized value → `bm25`
    (D-03), preserving the exact existing `recallByKeyword` behavior including
    the `recall_log`, `last_recall_query`, and `memory_salience` side effects.

    For `semantic` / `hybrid`: call `embedText(q)`. If the result is
    `degraded: true` (D-05: outage or provider disabled), fall back to the BM25
    path and return `{ results, query, mode, degraded: true, timestamp }` with
    HTTP 200 — never a 5xx. On a successful embedding, call `semanticRecall` or
    `hybridRecall` from `embeddings/recall.ts`, and return
    `{ results, query, mode, degraded: false, timestamp }`. Preserve the
    `agent_id`-filter and blank-query branches. The response stays backward
    compatible — `results` and `timestamp` keys unchanged, `mode` and `degraded`
    added.

    Run `gitnexus_impact` on the recall route `GET` handler before editing.
  </action>
  <verify>
    <automated>cd apps/memroos && npx tsc --noEmit -p . 2>&1 | (! grep -q 'recall/route') && echo "ROUTE TYPECHECK OK"</automated>
  </verify>
  <done>Recall route compiles; mode param honored; embedding outage returns degraded:true at HTTP 200; type check clean for the route file.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/recall | Untrusted `q` and `mode` query params cross into the recall handler. |
| recall handler → Ollama | Embedding compute call to local Ollama; may be unreachable. |
| background job → conversations.db | The scheduler writes derived embedding rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-04 | Denial of Service | `/api/recall` semantic path | mitigate | `embedText` has a 5s timeout and a degraded fallback; the endpoint returns BM25 + `degraded:true` at HTTP 200 instead of hanging or 5xx (D-05). |
| T-71-05 | Tampering | `mode` query param | mitigate | `mode` is whitelisted to `semantic|bm25|hybrid`; any other value falls through to BM25 (D-03) — no injection into a query path. |
| T-71-06 | Denial of Service | embedding background job | mitigate | Cycle is hard-capped at 50 messages (D-06); a degraded `embedText` short-circuits the cycle; job runs only under the scheduler-singleton lock so a single process owns it. |
| T-71-07 | Information disclosure | recall response | accept | Recall results expose `messages` content the caller is already authorized to recall via the existing endpoint; no new fields leak secrets. |
| T-71-SC | Tampering | npm installs | mitigate | No new npm packages introduced; no package-legitimacy checkpoint needed. |

</threat_model>

<verification>
- `npx vitest run src/lib/embeddings/__tests__/` — recall and embedding-job
  suites GREEN.
- `GET /api/recall?q=foo` with no `mode` returns identical BM25 results to the
  pre-change behavior (response includes `mode: "bm25"`).
- `GET /api/recall?q=foo&mode=semantic` with Ollama down returns HTTP 200 with
  `degraded: true`.
- `instrumentation.ts` registers `startEmbeddingJob` inside the
  `tryAcquireSchedulerLock()` block; no raw `setInterval` outside the singleton.
- TypeScript type check passes.
- Run `gitnexus_detect_changes()` before committing — verify changes match
  expected scope (recall route, instrumentation.ts, new embeddings/ files only).
</verification>

<success_criteria>
- `GET /api/recall` honors `mode=semantic|bm25|hybrid`; BM25 is the default.
- Hybrid mode fuses BM25 + semantic results via RRF (`k = 60`).
- Embedding outages and a disabled provider degrade to BM25 with `degraded:true`
  at HTTP 200 — never a 5xx.
- A background job embeds up to 50 un-embedded messages every 5 minutes via the
  scheduler singleton.
</success_criteria>

<output>Create `.planning/phases/71-recall-hil-sla-voice/71-02-SUMMARY.md` when done.</output>
