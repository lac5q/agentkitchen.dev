# Requirements: Agent Kitchen v1.6

## Monorepo

- [x] MONO-01: Kitchen app lives under `apps/kitchen`.
- [x] MONO-02: Service runtimes live under `services/`.
- [x] MONO-03: Root scripts delegate to `apps/kitchen`.
- [x] MONO-04: Runtime data remains rooted at `data/`.
- [x] MONO-05: App config lookup is root-aware with app-local fallback.

## MCP and Tool Attention

- [x] MCP-01: Root `.mcp.json` points memory at the monorepo service wrapper.
- [x] MCP-02: Root `.mcp.json` points Knowledge MCP at the monorepo service and preserves private Knowledge Hub root configuration.
- [x] MCP-03: Knowledge MCP exposes progressive core tools plus deeper workspaces.
- [x] TOOL-01: Tool Attention supports catalog, discover, load, record outcome, and stats actions.
- [x] TOOL-02: Outcome history writes to `logs/tool-attention-outcomes.jsonl`.

## Kitchen UI

- [x] UI-01: `/api/tool-attention` returns summary, capabilities, outcomes, recommendations, sources, health, and timestamp.
- [x] UI-02: Cookbooks page includes Tool Attention after Health Overview.
- [x] UI-03: Flow includes a Tool Gateway node.
- [x] UI-04: Node detail panel shows Tool Gateway stats and recent outcomes.

## Operations

- [x] OPS-01: Production build succeeds from the monorepo root.
- [x] OPS-02: Production LaunchAgent serves the merged monorepo build on port `3002`.
- [x] OPS-03: Monorepo CI validates Kitchen tests/build and Python service smoke tests.
