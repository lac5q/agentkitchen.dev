[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "memroos-eval-sdk"
version = "0.9.0"
description = "Python SDK for the MemroOS Public Eval API"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "respx>=0.21",
]

[tool.hatch.build.targets.wheel]
packages = ["memroos_eval_sdk"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
